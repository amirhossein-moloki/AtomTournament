[pytest]
DJANGO_SETTINGS_MODULE = tournament_project.settings
python_files = tests.py test_*.py *_tests.py
addopts = -q -x --maxfail=1 --strict-markers --disable-warnings
markers =
    asyncio: mark a test as asyncio.
