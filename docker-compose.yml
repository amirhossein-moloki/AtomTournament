services:
  web:
    build: .
    command: >
      sh -c "./docker-entrypoint.sh"
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    env_file:
      - .env
    depends_on:
      - db
      - redis

  db:
    image: postgres:14
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

  redis:
    image: redis:8.2-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 1s
      timeout: 3s
      retries: 5

  celery:
    build: .
    command: celery -A tournament_project worker -l info
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy

  celery-beat:
    build: .
    command: celery -A tournament_project beat -l info
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy

  nginx:
    build:
      context: ./nginx
    volumes:
      # Volume های موجود شما برای فایل‌های استاتیک و مدیا
      - static_volume:/app/staticfiles
      - media_volume:/app/media
      # Volume های جدید برای اشتراک‌گذاری با Certbot
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot
      # اتصال مستقیم فایل کانفیگ برای اعمال تغییرات SSL
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
      - "443:443" # پورت HTTPS اضافه شد
    depends_on:
      - web

  # سرویس جدید Certbot برای مدیریت گواهینامه‌های SSL
  certbot:
    image: certbot/certbot
    volumes:
      - certbot_conf:/etc/letsencrypt
      - certbot_www:/var/www/certbot

volumes:
  postgres_data:
  static_volume:
  media_volume:
  # Volume های جدید برای نگهداری دائمی اطلاعات Certbot
  certbot_conf:
  certbot_www:
