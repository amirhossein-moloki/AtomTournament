SECRET_KEY="your-super-secret-key-here"
DEBUG=True
DATABASE_URL="postgres://user:password@localhost:5432/tournament_db"
REDIS_URL="redis://localhost:6379/0"
STORAGE_BACKEND="local"

# CORS Origins (comma-separated)
CORS_ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# Zarinpal Payment Gateway
ZARINPAL_MERCHANT_ID="your-zarinpal-merchant-id"

# SMS.ir Service
SMSIR_API_KEY="your-sms-ir-api-key"
SMSIR_LINE_NUMBER="your-sms-ir-line-number"

# Email SMTP Server Settings
EMAIL_HOST="smtp.example.com"
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER="your-email@example.com"
EMAIL_HOST_PASSWORD="your-email-password"
DEFAULT_FROM_EMAIL="noreply@example.com"

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS="localhost,127.0.0.1"

# AWS S3 Storage Settings (if STORAGE_BACKEND is 's3')
AWS_ACCESS_KEY_ID="your-aws-access-key-id"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"
AWS_STORAGE_BUCKET_NAME="your-s3-bucket-name"
AWS_S3_REGION_NAME="your-s3-region"

# Zarinpal Sandbox Mode
ZARINPAL_SANDBOX=True
